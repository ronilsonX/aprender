# aprender

 # git init
   
  # git config --global user.name "roilson.stos@gmail. com"
  # git config --global user.email "ronilson"
   
  # git config --list
   
  # git remote 
  # git branch -r
  # git remote add ronilsonuser git@github.com:ronilsonuser/pop.gi
  # git remote -v
  # git remote show origin 
  # git commit -m "a"

  # git push origin main 
  
  
   
  # git init
  
  # rm -rf .git
  
  #  git config --global user.name "ronilsonuser"
  #  git config --global user.email "ronilson.stos@gmail.com"
   
  #  git config --global --add user.name ronilsonuser
  #  git config --global --add user.email ronilson.stos@gmail.com
   
  #  git config --global --list
   
  # git config --list --name-only
  #  git config --get user.name
  #  git config --get user.email
  
   
  # git remote 
  # git branch -r
  # git remote add ronilsonuser git@github.com:ronilsonuser/pop.gi
  # git remote -v
  # git remote show origin 
  # git commit -m "a"

  # git push origin main 
 
 
  
